diff -U 0 -rpN a/Source/ccib.f90 b/Source/ccib.f90
--- a/Source/ccib.f90	2024-02-15 10:38:07.692174400 +0900
+++ b/Source/ccib.f90	2024-02-19 17:48:04.791042000 +0900
@@ -16728,0 +16729 @@ LOGICAL :: ALL_FLG
+INTEGER :: N_SIZE
@@ -17020 +17021,3 @@ IF (N_MPI_PROCESSES>1) THEN
-      IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+      !IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+      CALL MPI_COMM_SIZE(MPI_COMM_MESH(NM),N_SIZE,IERR)
+      IF (N_SIZE == 0) CYCLE
diff -U 0 -rpN a/Source/main.f90 b/Source/main.f90
--- a/Source/main.f90	2024-02-15 10:38:08.283557600 +0900
+++ b/Source/main.f90	2024-02-19 17:47:29.094727800 +0900
@@ -1732,0 +1733,2 @@ SUBROUTINE EXCHANGE_GEOMETRY_INFO
+INTEGER :: N_SIZE
+
@@ -1736 +1738,3 @@ DO NM=1,NMESHES
-   IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+   !IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+   CALL MPI_COMM_SIZE(MPI_COMM_MESH(NM),N_SIZE,IERR)
+   IF (N_SIZE == 0) CYCLE
@@ -2280,0 +2285 @@ TYPE (EXTERNAL_WALL_TYPE), POINTER :: EW
+INTEGER :: N_SIZE
@@ -2290 +2295,3 @@ DO WHILE (ANY(SETUP_PRESSURE_ZONES_INDEX
-         IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+         !IF (MPI_COMM_MESH(NM)==MPI_COMM_NULL) CYCLE
+         CALL MPI_COMM_SIZE(MPI_COMM_MESH(NM),N_SIZE,IERR)
+         IF (N_SIZE == 0) CYCLE
