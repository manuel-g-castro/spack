from spack import *

class Kiertaa(Package):

    has_code = False

    version('1.0.0b')

    def setup_run_environment(self, env):
        env.prepend_path('PATH', self.prefix.bin)
        env.prepend_path('PYTHONPATH', self.prefix.site-packages)

    def install(self, spec, prefix):
        pass
